package com.example.androidtvapp.utils

import android.content.ContentResolver
import android.media.tv.TvContract
import android.net.Uri

object ContentProviderHelper {
    fun getChannels(contentResolver: ContentResolver): List<Any> {
        // Your implementation
        return emptyList()
    }
    
    fun getPreviewPrograms(contentResolver: ContentResolver): List<Any> {
        // Your implementation
        return emptyList()
    }
    
    fun getWatchNextPrograms(contentResolver: ContentResolver): List<Any> {
        // Your implementation
        return emptyList()
    }
}
